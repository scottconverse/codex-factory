[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "codex-factory-router"
version = "0.1.0"
description = "Local RouteLLM scoring adapter for Codex Factory."
requires-python = ">=3.11,<3.13"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["codex_factory_router*"]
