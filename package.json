{
  "name": "codex-factory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Budgeted boss-and-worker orchestration for Codex and qualified local models.",
  "scripts": {
    "test": "node --test",
    "check": "node --check scripts/run-worker.mjs && node --check scripts/build-site.mjs && node --check scripts/check-site.mjs && node --check scripts/check-release.mjs && node --check scripts/serve-site.mjs && node --test",
    "release:check": "node scripts/check-release.mjs",
    "site:build": "node scripts/build-site.mjs",
    "site:check": "node scripts/check-site.mjs",
    "site:serve": "node scripts/serve-site.mjs"
  },
  "engines": {
    "node": ">=24"
  }
}
